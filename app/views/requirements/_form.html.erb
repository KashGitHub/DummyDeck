<%= form_for([@noc, @noc.requirements.build]) do |f| %>
	<div class="field">
		<%= f.label :'Example Requirement' %> <br />
		<%= f.text_field :name %>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>