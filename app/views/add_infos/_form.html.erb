<%= form_for([@noc, @noc.add_infos.build]) do |f| %>
	<div class="field">
		<%= f.label :'Example Additional Info' %> <br />
		<%= f.text_field :name %>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>