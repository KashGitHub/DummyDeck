<%= form_for([@noc, @noc.titles.build]) do |f| %>
	<div class="field">
		<%= f.label :'Example Title' %> <br />
		<%= f.text_field :name %>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>