<%= form_for([@noc, @noc.duty_left_branches.build]) do |f| %>
	<div class="field">
		<%= f.label :'Example Duty Left Branch' %> <br />
		<%= f.text_field :name %>
	</div>

	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>