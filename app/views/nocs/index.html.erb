<h1>Listing nocs</h1>

<table>
  <tr>
    <th>Code</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @nocs.each do |noc| %>
  <tr>
    <td><%= noc.code %></td>
    <td><%= noc.description %></td>
    <td><%= link_to 'Show', noc %></td>
    <td><%= link_to 'Edit', edit_noc_path(noc) %></td>
    <td><%= link_to 'Destroy', noc, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Noc', new_noc_path %>
